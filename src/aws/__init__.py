# FlashBalanceAI — src/aws package
# Contents:
#   cloudwatch_collector.py  — Lambda: CloudWatch state collector (Issue #20)
#   inference_server.py      — EC2 PPO inference server (Issue #21)
#   inference_coordinator.py — Lambda: lightweight inference coordinator (Issue #21)
#   scaling_trigger.py       — Lambda: ASG scale-out/in (Issue #22)
#   deploy.py                — boto3 idempotent infrastructure setup (Issue #17–#18)
#   iam_setup.py             — IAM roles and policies (Issue #15)
