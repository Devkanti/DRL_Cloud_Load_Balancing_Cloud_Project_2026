# ADR-001 — FlashBalanceAI Architecture Decision Record

**Status:** 🔴 DRAFT — Awaiting team sign-off  
**Issue:** GitHub Issue #1 (Phase 0 — Design Lock)  
**Created:** 2026-08-20  
**Authors:** Devkanti Sarkar · Agrima Gupta · Mohar Gorai · [4th member]  
**Source documents:** `PRD.md` (v1.0) · `IMPLEMENTATION_PLAN_PART1.md` (v1.0) · `IMPLEMENTATION_PLAN_PART2.md` (v1.0)

> **How to sign off:** Each team member must read every decision in this file, confirm or raise an objection in the GitHub Issue #1 thread, then add their name and student ID to the [Sign-off Checklist](#sign-off-checklist) below by committing directly to `decisions/ADR-001.md` on a named branch and opening a PR.  
> **This file is locked for code-level changes until all four sign-offs are recorded.**

---

## Sign-off Checklist

Each member must tick every box before closing Issue #1.  
Replace `[ ]` with `[x]` in your own commit.

| Member | Student ID | Read PRD §7–12 | Reviewed all decisions | Raised objections | Signed off |
|--------|------------|:--------------:|:---------------------:|:-----------------:|:----------:|
| Devkanti Sarkar | 24BIT0162 | [ ] | [ ] | [ ] or N/A | [ ] |
| Agrima Gupta | 24BIT0253 | [ ] | [ ] | [ ] or N/A | [ ] |
| Mohar Gorai | TBD | [ ] | [ ] | [ ] or N/A | [ ] |
| [4th member] | TBD | [ ] | [ ] | [ ] or N/A | [ ] |

**Issue #1 may only be closed when all four rows show `[x]` in the Signed off column.**

---

## Decision Index

| # | Decision | Status |
|---|----------|--------|
| D1 | Primary DRL Algorithm | ✅ Locked in PRD |
| D2 | Primary DRL Baseline | ✅ Locked in PRD |
| D3 | Rejected Algorithms (DDPG, H-MAS) | ✅ Locked in PRD |
| D4 | MDP Action Space | ✅ Locked in PRD |
| D5 | MDP State Space | ✅ Locked in PRD |
| D6 | Reward Function Weights | ⚠️ Provisional — subject to E10 ablation |
| D7 | Neural Network Architecture | ✅ Locked in PRD |
| D8 | Training Hyperparameters | ⚠️ Provisional — subject to tuning |
| D9 | Training Location | ⚠️ Needs team confirmation |
| D10 | AWS Region | ✅ Locked in PRD |
| D11 | EC2 Instance Configuration | ✅ Locked in PRD |
| D12 | AWS Service Inventory | ✅ Locked in PRD |
| D13 | Excluded AWS Services | ✅ Locked in PRD |
| D14 | PPO Inference Deployment | ⚠️ Needs team confirmation |
| D15 | Traffic Generator Strategy | ✅ Locked in PRD |
| D16 | Primary Dataset | ✅ Locked in PRD |
| D17 | Alibaba Trace Usage | ✅ Locked in PRD |
| D18 | Train/Validation/Test Seed Split | ✅ Locked in PRD |
| D19 | Non-DRL Baselines | ✅ Locked in PRD |
| D20 | Experiment Scope (E1–E10) | ✅ Locked in PRD |
| D21 | Statistical Analysis Protocol | ✅ Locked in PRD |
| D22 | AWS Total Budget Ceiling | ⚠️ Needs team confirmation |
| D23 | Repository Branch Structure | ✅ Already implemented |
| D24 | Python Environment | ✅ Locked in implementation plan |

---

## Decisions

---

### D1 — Primary DRL Algorithm

**Decision:** **PPO (Proximal Policy Optimization)** via Stable-Baselines3 v2.3.0.

**Rationale (from PRD §10.1):**
- PPO's clipped surrogate objective (`ε=0.2`) prevents policy collapse under high reward variance — a critical property for flash-sale traffic where reward signals swing dramatically between phases.
- Two of the five surveyed papers (Funika et al. 2023, Femminella & Reali 2024) validate PPO for cloud-related tasks.
- PPO supports parallel vectorised training (`VecEnv`) without DDPG's single-environment limitation.
- PPO works with both discrete and continuous action spaces; our routing problem is naturally discrete.

**Alternatives considered and rejected:**  
→ DQN: retained as baseline only (see D2)  
→ DDPG: rejected (see D3)  
→ H-MAS: rejected (see D3)

**Status:** ✅ Locked — no changes permitted without raising a new ADR.

---

### D2 — Primary DRL Baseline

**Decision:** **DQN (Deep Q-Network)** — Mnih et al. (2015), Stable-Baselines3 implementation.

**Architecture:** Identical 2-layer MLP [64, 64] to PPO for a fair comparison. Experience replay buffer size 100,000. Target network updated every 1,000 steps.

**Rationale (from PRD §10.2):**
- Agrima's Phase-I document explicitly proposes a DQN vs PPO comparison as its core novelty contribution.
- Zhou et al. (2024, Elsevier) is the only surveyed paper that compares both algorithms — but in an SDN simulation, not AWS, and not under flash-sale traffic. This project closes that gap (Research Gap G3).
- DQN is the most widely used DRL baseline for discrete routing problems in the literature.

**Status:** ✅ Locked — no changes permitted without raising a new ADR.

---

### D3 — Rejected Algorithms (DDPG and H-MAS)

**Decision:** DDPG and H-MAS are **NOT implemented** as primary algorithms. They are retained only as related-work discussion.

**DDPG rejection rationale (from PRD §10.3):**
1. Our ALB routing is inherently discrete; DDPG requires continuous actions, adding unnecessary continuous-to-discrete mapping complexity.
2. DDPG is hyperparameter-sensitive and slower to converge — an unacceptable risk for a student-budget project.
3. No surveyed paper demonstrates DDPG outperforming PPO for request-level routing under burst traffic.

**H-MAS rejection rationale (from PRD §10.4):**
1. H-MAS requires designing and coordinating multiple agents — doubles implementation complexity for uncertain benefit.
2. The key H-MAS noise-filtering benefit is preserved via the `burst_indicator` feature in the state space.
3. No empirical evidence from the surveyed papers supports H-MAS for flash-sale routing specifically.

**Mohar's contribution preserved via:**  
The `burst_indicator` state feature (`arrival_rate / λ_baseline > 2 → 1`) is a direct, simplified proxy for the noise-filtering role of the H-MAS supervisor layer. Mohar's H-MAS insight is thus architecturally embedded, not discarded.

**Status:** ✅ Locked — no changes permitted without raising a new ADR.

---

### D4 — MDP Action Space

**Decision:** **Discrete action space, A = {0, 1, 2, 3}** — select one of N=4 backend EC2 instances as the routing target for the current 100 ms time window.

**Implementation detail:** At inference, the chosen instance receives the majority of traffic for the current window via ALB target group weight updates. Sticky sessions disabled.

**N is configurable** in `FlashSaleEnv(n_instances=N)` and `configs/ppo_config.yaml`. The locked value for experiments is N=4 (student account quota constraint).

**Cross-reference:** PRD §9.3 — full justification for discrete over continuous.

**Status:** ✅ Locked.

---

### D5 — MDP State Space

**Decision:** **23-dimensional state vector** `s_t ∈ ℝ^23` for N=4 instances (general formula: 5N+3).

```
s_t = [
  cpu_util[0..3],           # CloudWatch CPUUtilization, normalised 0–1        (4 dims)
  active_conn[0..3],        # ALB ActiveConnectionCount, normalised 0–1        (4 dims)
  queue_depth[0..3],        # Custom FlashBalanceAI/Instances QueueDepth       (4 dims)
  resp_time_ema[0..3],      # ALB TargetResponseTime EMA, normalised 0–1       (4 dims)
  health_status[0..3],      # ALB health check: 1=healthy, 0=unhealthy         (4 dims)
  arrival_rate_norm,        # λ(t)/λ_baseline, range [0, 10+]                  (1 dim)
  burst_indicator,          # 1 if λ(t)/λ_baseline > 2, else 0                (1 dim)
  time_since_last_spike     # Normalised seconds since last burst onset, 0–1   (1 dim)
]
```

**State sources (AWS runtime):**
- `cpu_util`: CloudWatch `AWS/EC2 CPUUtilization` (custom 10s resolution)
- `active_conn`: CloudWatch `AWS/ApplicationELB ActiveConnectionCount` per target
- `queue_depth`: Custom CloudWatch metric `FlashBalanceAI/Instances QueueDepth` published by Flask backend
- `resp_time_ema`: CloudWatch `AWS/ApplicationELB TargetResponseTime` (EMA computed in Lambda)
- `health_status`: ALB target group health check API
- `arrival_rate_norm`, `burst_indicator`, `time_since_last_spike`: derived in Lambda state collector

**Cross-reference:** PRD §9.2 for full derivation.

**Status:** ✅ Locked.

---

### D6 — Reward Function Weights

**Decision (provisional):** Four-component reward with initial weights:

```
R(t) = 0.4 · R_lat(t) + 0.2 · R_util(t) + 0.2 · R_tput(t) + 0.2 · R_sla(t)
```

| Component | Weight | Formula | Rationale |
|-----------|--------|---------|-----------|
| R_lat | w₁ = 0.40 | `−(avg_resp_time / 200ms) + 1` | Primary user-facing SLA metric |
| R_util | w₂ = 0.20 | `avg_cpu − |avg_cpu − 0.70|` | Penalises both under- and over-utilisation |
| R_tput | w₃ = 0.20 | `requests_served / requests_arrived` | Ensures requests are served, not just routed |
| R_sla | w₄ = 0.20 | `−2.0 · I(P95_latency > 500ms)` | Hard guardrail on tail latency |

**Targets:** latency_target = 200ms, util_target = 70%, SLA_threshold = 500ms, SLA penalty k = 2.0.

**⚠️ PROVISIONAL:** These weights are the starting point. Experiment E10 (ablation study) will vary w₂ across {0.10, 0.20, 0.40} and zero out each component in turn. Claims about optimal weights must wait for E10 results. Do NOT treat 0.4/0.2/0.2/0.2 as "proven optimal".

**Cross-reference:** PRD §9.4 for full component derivation and literature citations.

**Status:** ⚠️ Provisional — initial training values locked; E10 may result in an updated ADR.

---

### D7 — Neural Network Architecture

**Decision:** **Shared 2-layer MLP [64, 64]** for both PPO actor-critic and DQN.

| Property | Value |
|----------|-------|
| Hidden layers | 2 |
| Hidden units per layer | 64 |
| Activation | ReLU |
| PPO actor output | Softmax over 4 actions |
| PPO critic output | Scalar value V(s) |
| DQN output | Q-values for 4 actions |
| Input | 23-dimensional state vector |

**Justification:** Identical architecture for PPO and DQN is required for a fair comparison (D2). [64, 64] is the Stable-Baselines3 default and is sufficient for a 23-dim input with 4-action output. Increasing to [128, 128] is an optional future extension if convergence is slow.

**Status:** ✅ Locked for initial experiments.

---

### D8 — PPO Training Hyperparameters

**Decision (provisional):** Initial hyperparameters from `configs/ppo_config.yaml`:

| Parameter | Value | Basis |
|-----------|-------|-------|
| Learning rate | 3 × 10⁻⁴ (Adam) | Schulman et al. (2017) standard |
| Rollout steps K | 2048 | SB3 default |
| Mini-batch size | 64 | Standard |
| Update epochs | 10 | PPO paper |
| Clip ratio ε | 0.2 | PPO paper |
| GAE λ | 0.95 | Standard |
| Discount γ | 0.99 | Long-horizon |
| Entropy coeff c₂ | 0.01 | Mild exploration |
| Value loss coeff c₁ | 0.5 | Standard |
| Total training steps | 2,000,000 | Monitor for convergence |
| Parallel envs (VecEnv) | 4 | CPU-bound; 4 cores typical |

**DQN-specific parameters:** `configs/dqn_config.yaml` — buffer_size=100,000, learning_starts=10,000, target_update_interval=1,000, exploration_fraction=0.15, exploration_final_eps=0.05.

**⚠️ PROVISIONAL:** These are starting values. If PPO fails to converge during the local evaluation gate check (Issue #13), hyperparameter tuning is permitted on validation seeds 112–126. No changes are permitted to the test-set evaluation protocol after experiments begin.

**Status:** ⚠️ Provisional — may be adjusted before training locks in (Issue #11).

---

### D9 — Training Location

**Decision (pending confirmation):** **Local laptop CPU first**. SageMaker (`ml.t2.medium`) used only if local training fails or takes > 8 hours per run.

**Expected local training time:** ~3–4 hours for 2M steps with `n_envs=4` on a 4-core laptop.  
**SageMaker fallback cost:** ~$0.046/hr × 4 hrs = ~$0.20 per training run.

**⚠️ NEEDS TEAM CONFIRMATION:**
> Does any team member NOT have a laptop capable of running Python 3.11 + PyTorch 2.2.2 + SB3 for 3–4 hours? If so, SageMaker must be the primary training location, which changes the cost budget.
>
> **Please confirm in Issue #1 comments:** "My laptop can handle local training" or "I need SageMaker".

**Status:** ⚠️ Pending — Issue #1 comment confirmation required from all 4 members.

---

### D10 — AWS Region

**Decision:** **us-east-1 (N. Virginia)** for all AWS resources.

**Rationale:** Lowest cost, widest service availability, recommended for student accounts by AWS Educate/Academy guidelines.

**All resource names in `configs/aws_config.yaml` assume us-east-1.** If the team's AWS account is restricted to a different region, this must be raised as an objection in Issue #1 before any resources are provisioned.

**Status:** ✅ Locked — unless account restriction forces a change (raise in Issue #1).

---

### D11 — EC2 Instance Configuration

**Decision:**

| Role | Count | Type | Constraint |
|------|-------|------|------------|
| Backend servers | 4 (desired), min=2, max=8 | t2.micro | Free Tier: 750 hrs/mo total |
| PPO inference server | 1 | t2.micro | Stopped when not experimenting |
| **Total maximum** | **5** t2.micro running simultaneously | | Must not exceed student account quota |

**ASG configuration:** `FlashBalanceAI-ASG`, min=2, desired=4, max=8, region=us-east-1.

**⚠️ PREREQUISITE:** Before provisioning (Issue #14/Issue #17), verify that the student AWS account has an EC2 t2.micro quota of ≥ 5 in us-east-1. If quota is ≤ 4, request an increase or redesign for N=3.

**Cost guard:** All EC2 instances must be stopped (not terminated — to preserve EBS) when no experiments are running. Auto-stop after 4 hours of idle can be configured via Lambda.

**Status:** ✅ Locked for N=4. Contingency: if quota prevents 5 simultaneous instances, reduce to N=3 and raise a follow-up ADR.

---

### D12 — AWS Service Inventory (Minimum Required Set)

**Decision:** The following services are approved for use. No other AWS services may be added without a follow-up ADR.

| Service | Resource | Role |
|---------|----------|------|
| EC2 | t2.micro × up to 5 | Backend servers + inference server |
| ALB | 1 instance | HTTP traffic routing |
| Lambda | 3 functions | State collector, scaling trigger, inference coordinator |
| CloudWatch | Custom namespace `FlashBalanceAI/*` | Metrics, alarms, dashboard |
| S3 | 1 bucket `flashbalanceai-{account_id}` | Models, state, results |
| DynamoDB | 1 table `routing_decisions` | Routing audit log |
| API Gateway | 1 REST API | JMeter traffic entry point |
| ASG | 1 group | Horizontal EC2 scaling |
| IAM | 2 roles | Lambda role, EC2 role (least-privilege) |
| SNS | 1 topic | Billing alarm emails |

**Cross-reference:** PRD §12.1 for cost breakdown. Implementation plan Part 2 Final Architecture for exact exclusions.

**Status:** ✅ Locked.

---

### D13 — Excluded AWS Services

**Decision:** The following services proposed in team members' Phase-I documents are **explicitly excluded** from this project scope:

| Service | Proposed by | Reason for exclusion |
|---------|------------|----------------------|
| SageMaker (notebook) | All | Local training preferred; SageMaker only as fallback (see D9) |
| RDS | — | Backend is stateless Flask; no DB layer needed for experiments |
| EFS | — | No shared filesystem required |
| SQS | — | Lambda + S3 state file is sufficient for state passing |
| Cognito | Agrima | No authentication needed for controlled experiments |
| CloudFront | Agrima, PRD §12.1 (optional) | CDN not needed for backend load testing |
| ElastiCache | — | Not required for current scope |

**Rationale:** Minimising services reduces cost, complexity, and blast radius if something goes wrong. Any service added later requires a new ADR entry.

**Status:** ✅ Locked.

---

### D14 — PPO Inference Deployment Approach

**Decision (pending confirmation):** Use a **dedicated EC2 t2.micro inference server** running `src/aws/inference_server.py` (Flask app), called by a lightweight Lambda coordinator every 30s.

**Background — why not pure Lambda:**  
Stable-Baselines3 + PyTorch unzipped exceeds AWS Lambda's 250MB unzipped package size limit. Options:
- **Option A (chosen):** EC2 t2.micro inference server with SB3 pre-installed. Lightweight Lambda reads S3 state, calls `/infer` endpoint, updates ALB. SB3 model loaded at EC2 startup from S3.
- **Option B:** Lambda Layer with SB3 pre-compiled for Lambda runtime. Technically feasible but fragile and harder to debug.
- **Option C:** SageMaker endpoint. Costs ~$0.046/hr when active — acceptable but adds service complexity.

**⚠️ NEEDS TEAM CONFIRMATION:**
> Option A (EC2 inference server) is the recommended approach. Does any team member foresee a problem with this? Specifically: will this extra t2.micro instance push us over the Free Tier 750 hr/month quota when combined with 4 backend instances?
>
> **Calculation:** 5 instances × 24 hrs × 30 days = 3,600 hrs/month vs. Free Tier 750 hrs. This will exceed Free Tier if all instances run continuously. **Mitigation:** stop the inference server and backend instances when not running experiments.
>
> **Please confirm in Issue #1:** "Option A is acceptable with stop/start discipline" or propose Option B/C.

**Status:** ⚠️ Pending — Issue #1 comment confirmation required.

---

### D15 — Traffic Generator Strategy

**Decision:**
1. **Primary load injection:** Apache JMeter 5.6.3 running locally against ALB DNS.
2. **Traffic profile generator:** Custom Python `src/traffic/traffic_generator.py` — produces normalised arrival-rate time series with configurable burst multipliers and reproducible seeds.
3. **Integration:** JMeter reads arrival-rate profile from `configs/traffic_config.yaml`; ramps thread count to match the profile.

**Traffic profile phases (per episode, 13 minutes total):**

| Phase | Duration | Rate | Multiplier |
|-------|----------|------|------------|
| Warm-up | 2 min | 100 req/s | 1× |
| Pre-announcement | 1 min | 300 req/s | 3× |
| Spike onset | 10 sec | 0 → 1000+ req/s | linear ramp |
| Peak | 5 min | configurable | 10×, 50×, 100× |
| Cooldown | 5 min | exponential decay | → 1× |

**Noise:** 10% Gaussian noise added to simulate transient spikes within the burst.

**Cross-reference:** PRD §9.8–§9.9 for burst modelling details.

**Status:** ✅ Locked.

---

### D16 — Primary Dataset

**Decision:** **FlashSale-Traffic-Synthetic v1.0** — generated by `src/traffic/traffic_generator.py` with fixed seeds. This is the **only** dataset used for PPO/DQN training, validation, and test evaluation.

**Rationale:** Reproducible, calibrated, avoids legal/privacy issues. Real production data (Amazon Prime Day, Alibaba 11.11) is referenced in the paper introduction for motivation but not used directly.

**Status:** ✅ Locked.

---

### D17 — Alibaba Cluster Trace Usage

**Decision:** The Alibaba Cloud Cluster Trace (v2018) is used **only** for:
- Supplementary arrival-rate distribution comparison in the paper
- Optional agent pre-training warm-start (if convergence is slow)

**⚠️ Explicit restriction:** The Alibaba trace is batch job scheduling data, NOT e-commerce request routing data. It must NOT be cited as validation evidence for flash-sale performance. Any result claiming to validate flash-sale behaviour MUST use `FlashSale-Traffic-Synthetic v1.0`.

**Cross-reference:** PRD §11.1 integrity note; Implementation Plan Part 1 T2.3.

**Status:** ✅ Locked.

---

### D18 — Train/Validation/Test Seed Split

**Decision:**

| Split | Seeds | Episodes | Usage |
|-------|-------|----------|-------|
| Training | 42–111 | 70 | PPO/DQN agent training |
| Validation | 112–126 | 15 | Hyperparameter tuning, EvalCallback checkpoint selection |
| Test | 127–141 | 15 | Final reported results only |

**Data-leakage rules (non-negotiable):**
- Scaler statistics (mean, std for normalisation) derived from training episodes only.
- Test seeds 127–141 are not used during any training or hyperparameter tuning step.
- Validation seeds are used only for checkpoint selection — never for reported metrics.
- PPO and DQN must use the same test seeds for a fair comparison.

**Cross-reference:** PRD §11.4–§11.5.

**Status:** ✅ Locked — violation of these rules invalidates reported results.

---

### D19 — Non-DRL Baselines

**Decision:** Five non-DRL baselines, all implemented in software:

| Baseline | Location | Notes |
|----------|----------|-------|
| Round Robin (RR) | `src/baselines/round_robin.py` | Cyclic allocation |
| Weighted Round Robin (WRR) | `src/baselines/weighted_round_robin.py` | Weight ∝ 1/cpu_util |
| Least Connections (LC) | `src/baselines/least_connections.py` | Route to min active_conn |
| Threshold Autoscaler | `src/baselines/threshold_autoscaler.py` | Mimics CloudWatch CPU>70% alarm logic |
| Random | Inline in eval loop | Lower bound sanity check |

**Not included as baselines:** DDPG, H-MAS (see D3).

**Status:** ✅ Locked.

---

### D20 — Experiment Scope

**Decision:** Ten experiments E1–E10 as defined in `IMPLEMENTATION_PLAN_PART2.md` Phase 6 and `PRD.md` §24.

| Experiment | Type | Min Reps | Critical Path |
|-----------|------|----------|---------------|
| E1: Baseline (1× load) | Calibration | 5 | ✅ Yes |
| E2: 10× spike | Primary | 5 | ✅ Yes |
| E3: 50× spike | Stress | 5 | No |
| E4: 100× spike | Conditional | 3 | No (conditional on E3) |
| E5: Repeated bursts | Recovery | 5 | No |
| E6: Noisy spikes | Noise robustness | 5 | No |
| E7: Cold-start time | Scaling speed | 5+5 | No |
| E8: Cost/perf trade-off | Ablation | 3/config | No |
| E9: PPO vs DQN | Comparison | Reuses E2/E3 | No |
| E10: Reward ablation | Ablation | 3/variant | ✅ Yes (for paper) |

**No results from any experiment may be reported in the paper until:** (a) minimum repetitions completed, (b) all raw data committed, (c) statistical analysis in Issue #36 is complete.

**Status:** ✅ Locked.

---

### D21 — Statistical Analysis Protocol

**Decision:** All reported results must follow this protocol:

- **Repetitions:** N_rep ≥ 5 per experiment per algorithm
- **Central tendency:** Mean across repetitions
- **Spread:** Standard deviation (ddof=1)
- **Confidence interval:** 95% CI using t-distribution (n-1 degrees of freedom)
- **Significance test:** Paired t-test, PPO vs each baseline, α = 0.05
- **Effect size:** Cohen's d alongside p-values
- **No cherry-picking:** All N_rep runs reported; no post-hoc seed filtering

**Cross-reference:** PRD §14.2.

**Status:** ✅ Locked — deviating from this protocol invalidates statistical claims.

---

### D22 — AWS Total Budget Ceiling

**Decision (pending confirmation):** **$20 USD hard ceiling** for total AWS spend across the entire project.

**Expected breakdown:**
- Phase 4–5 setup and testing: ~$1–3
- Phase 6 experiments (E1–E10): ~$8–15
- Demo/teardown buffer: ~$2–2
- **Total estimate: $15–35** (PRD §2, Implementation Plan Part 2 timeline)

**⚠️ NEEDS TEAM CONFIRMATION:**
> The implementation plan estimates $15–35 total. The $20 ceiling may be tight if E5 (repeated bursts, ~$1.50) and E8/E10 (retraining runs) all run as planned. Is the team prepared to contribute to AWS costs beyond the student credit allocation if needed? Who holds the AWS account?
>
> **Please confirm in Issue #1:**
> 1. Which team member's account will be used?
> 2. What is the available student credit balance?
> 3. Is $20 the agreed ceiling, or should it be raised to $35?

**Billing safeguards (non-negotiable regardless of ceiling decision):**
- CloudWatch billing alarm at $5 threshold → SNS email
- AWS Budgets hard ceiling configured before first chargeable resource
- All EC2/ALB stopped immediately after each experiment session
- Teardown script (Issue #46) executed before final submission

**Status:** ⚠️ Pending — credit balance and account owner must be confirmed in Issue #1.

---

### D23 — Repository Branch Structure

**Decision:** Three-tier branch model (already implemented):

```
main          ← stable, tagged releases only
  └── develop ← integration branch; all features merge here
        └── feature/phase{N}-{description}   ← individual work
        └── fix/issue{N}-{description}
        └── experiment/e{N}-{algorithm}
        └── docs/phase{N}-{document}
```

**This structure is already in place. No changes needed.**

**Commit message convention:** `[PHASE-{N}] {verb} {what}` — e.g., `[PHASE-1] implement FlashSaleEnv with 23-dim state space`.

**Minimum GitHub participation per BCSE355L guidelines:** ≥ 2 PRs opened and ≥ 2 PR code reviews per team member; weekly commits; continuous documentation updates.

**Status:** ✅ Already implemented.

---

### D24 — Python Environment

**Decision:** Python **3.11** (not 3.12 — confirmed SB3 2.3.0 compatibility).

**Pinned package versions (from `IMPLEMENTATION_PLAN_PART1.md` T1.1):**

| Package | Version |
|---------|---------|
| stable-baselines3 | 2.3.0 |
| gymnasium | 0.29.1 |
| torch | 2.2.2 |
| numpy | 1.26.4 |
| pandas | 2.2.1 |
| boto3 | 1.34.84 |
| flask | 3.0.3 |
| dash | 2.17.0 |
| plotly | 5.22.0 |
| pytest | 8.1.1 |
| pytest-cov | 5.0.0 |
| pyyaml | 6.0.1 |
| requests | 2.31.0 |
| locust | 2.24.1 |

**Environment files:** `environment.yml` (conda) and `requirements.txt` (pip) — both committed to repo root, both must produce identical package trees.

**Verification command:** `python -c "import stable_baselines3; import gymnasium; print('OK')`

**Status:** ✅ Locked — do not upgrade package versions mid-project without team consensus and a new ADR entry.

---

## Open Items Requiring Team Resolution

The following items are marked ⚠️ above and MUST be resolved in the Issue #1 thread before Phase 1 work begins:

| # | Item | Decision Needed By |
|---|------|--------------------|
| OI-1 | **D9 — Training Location:** Does every team member have a capable laptop, or is SageMaker required as primary? | Before Issue #11 (PPO training) starts |
| OI-2 | **D14 — Inference Deployment:** Confirm Option A (EC2 inference server) is acceptable; confirm stop/start cost discipline is agreed | Before Issue #21 (inference deploy) starts |
| OI-3 | **D22 — AWS Budget:** Confirm which member's account is used, available credit balance, and agreed ceiling ($20 vs $35) | Before Issue #14 (AWS billing alarm) |

**All three open items must be resolved and documented as comments in Issue #1 before the issue is closed.**

---

## Change Log

| Version | Date | Author | Change |
|---------|------|--------|--------|
| 0.1 (draft) | 2026-08-20 | (generated from PRD + implementation plan) | Initial draft — all decisions extracted from source documents |

*After sign-off, all changes to this document require a new PR, a new row in this change log, and re-acknowledgement from at least 2 team members.*

---

*This document is derived exclusively from `PRD.md` v1.0, `IMPLEMENTATION_PLAN_PART1.md` v1.0, and `IMPLEMENTATION_PLAN_PART2.md` v1.0. It does not introduce new architectural decisions — it records and organises decisions already made in those documents. Section references (e.g., "PRD §9.4") are provided for traceability.*
